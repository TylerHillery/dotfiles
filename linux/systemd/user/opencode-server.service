[Unit]
Description=OpenCode HTTP server
After=network-online.target

[Service]
Type=simple
WorkingDirectory=%h
ExecStart=/bin/bash -lc 'exec "$HOME/.local/bin/mise" exec -- opencode serve --hostname 127.0.0.1 --port 4096'
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
